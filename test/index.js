/*global it: true, describe: true */
/*eslint no-console: 0*/

import should from 'should'
import * as forms from '../src'

describe('shasta-forms', () => {
  it('should exist', (done) => {
    should.exist(forms)
    done()
  })
})
